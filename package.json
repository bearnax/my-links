{
  "name": "my-links",
  "version": "1.0.0",
  "description": "Will's personal link hub — a static site built from data/links.json",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "sync": "python3 scripts/sync-links.py",
    "test": "npm run build && python3 scripts/doctor.py",
    "lint": "echo 'No linter configured'"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
